export default class CurrencyPairPosition {
    
    constructor(symbol, basePnl, baseTradedAmount) {
        this.symbol = symbol;
        this.basePnl = basePnl;
        this.baseTradedAmount = baseTradedAmount;
    }
}
